import { Layout } from 'antd';
import React, { FC } from 'react';

const FooterApp: FC = () => {
	return (
		<Layout.Footer>
			<span>This is footer</span>
		</Layout.Footer>
	);
};

export default FooterApp;
